# Database Configuration (application uses local SQLite by default)
# If you later want to enable a remote Postgres DB, add your JDBC settings
# here or switch the DatabaseConnection implementation.
db.user=postgres
db.name=postgres
db.port=5432